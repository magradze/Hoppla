import SearchBox from "@/components/partial/SearchBox";

const CarPool = async () => {
    return (
        <div className="relative isolate z-10">
            <SearchBox className="page-wrapper mt-8" type="carpool"/>
            <div className="page-wrapper">

            </div>
        </div>
    );
};

export default CarPool;